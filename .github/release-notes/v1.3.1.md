## 更新内容

- 点击“开启 Cross-SIM”时，会在开启用户开关后同步写入普通 CarrierConfig（`persistent=false`）。即使设备不支持持久化配置，也可以尝试在当前开机周期应用 Cross-SIM 配置。
- “关闭 Cross-SIM”仍只关闭用户开关，不会清除 CarrierConfig。
- 开启、关闭、读取、持久化、清除、权限及错误提示全部改为弹窗显示，操作结果更加醒目。
- 移除主页面底部的结果文本区域，让操作界面更简洁。
- 原有的持久化 CarrierConfig 和“一键开启并持久化”功能保持不变。
