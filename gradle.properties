org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# Required when the project lives under a Windows user profile with non-ASCII characters.
android.overridePathCheck=true

# These defaults can be overridden with -PappId/-PappVersionCode/-PappVersionName
# or the APP_ID/APP_VERSION_CODE/APP_VERSION_NAME environment variables.
appId=com.example.crosssim
appVersionCode=1003001
appVersionName=1.3.1
